q3; % this runs the SIS Algorithm
q4; % this runs the SISR Algorithm
q5a; % this runs the SISR algorithm with best alpha=0.9
%q5; this runs the liklihood calculation on range of alpha values (1 iteration only    )